# EC2 Setup Guide

- Update the system packages.
- Install Python 3.11 and pip.
- Clone the repository and install the backend dependencies.
- Configure a systemd service for the FastAPI application.
