# AWS Deployment Guide

1. Provision an EC2 instance with Ubuntu.
2. Install Python, Nginx, and Docker if needed.
3. Deploy the FastAPI backend and configure Nginx as a reverse proxy.
4. Use S3 for storing uploaded images and model artifacts.
